interface:
  display_name: "Decision Surface Discovery"
  short_description: "Find bounded decisions for JEV"
  default_prompt: "Use $action-vocabulary-forge to find bounded decision surfaces and compile a JEV-ready bundle from this system."
