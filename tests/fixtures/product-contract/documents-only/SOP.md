# Support request handling

Review the request using only the information available when it arrived.

Choose exactly one disposition:

- `answer` when the supplied information is sufficient;
- `request_information` when required facts are missing;
- `escalate` when policy or risk requires a specialist.
