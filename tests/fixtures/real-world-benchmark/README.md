# Real-world benchmark V1 corpus

This frozen corpus contains only minimal excerpts needed to test discovery.
Each excerpt is attributed in `manifest.yaml`; the benchmark never fetches the
network. The excerpts are test inputs, not a claim that the Forge understands
the complete upstream repositories.
